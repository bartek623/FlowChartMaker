// time to rerender snap lines when element is dragged
export const TIME_TO_RERENDER_SNAP_LINES = 100;
// 100 ms is enough, only on fast changing element position delay is visible

// distance in px to snap element to snap line
export const SNAP_MARGIN = 10;

// distance in px (from cursor) in which snap lines should show up
export const SHOW_SNAP_LINES_DISTANCE = 100;
